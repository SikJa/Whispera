{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local settings window and native file selection only.",
  "windows": ["main", "recorder", "details", "import"],
  "permissions": ["core:default", "core:window:allow-start-dragging", "core:window:allow-hide", "dialog:allow-open"]
}
